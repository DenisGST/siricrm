# =============================================================================
# Production stack для SiriCRM
#
# Запуск:
#   docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
#
# Перед первым запуском:
#   1. Заполнить .env.prod (скопировать из .env.prod.example)
#   2. Получить SSL сертификат: ./scripts/init-letsencrypt.sh
#   3. Применить миграции: docker compose -f docker-compose.prod.yml exec web python manage.py migrate
# =============================================================================

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    expose:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"

  redis:
    image: redis:7-alpine
    expose:
      - "6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  web:
    build: .
    command: >
      bash -c "
        python manage.py collectstatic --noinput &&
        python manage.py migrate --noinput &&
        daphne -b 0.0.0.0 -p 8000 --proxy-headers config.asgi:application
      "
    volumes:
      - .:/app
      - static_files:/app/staticfiles
    expose:
      - "8000"
    env_file:
      - .env.prod
    environment:
      DJANGO_ENV: prod
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "5"

  celery:
    build: .
    command: celery -A config worker -l info --max-tasks-per-child=100 --time-limit=300 -E
    volumes:
      - .:/app
    env_file:
      - .env.prod
    environment:
      DJANGO_ENV: prod
    depends_on:
      - db
      - redis
      - web
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"

  celery-beat:
    build: .
    command: celery -A config beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
    volumes:
      - .:/app
    env_file:
      - .env.prod
    environment:
      DJANGO_ENV: prod
    depends_on:
      - db
      - redis
      - web
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  userbot:
    build: .
    command: python manage.py run_userbot
    volumes:
      - .:/app
    env_file:
      - .env.prod
    environment:
      DJANGO_ENV: prod
    depends_on:
      - db
      - redis
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  nginx:
    image: nginx:1.27-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/proxy_params.conf:/etc/nginx/proxy_params.conf:ro
      - ./nginx/templates:/etc/nginx/templates:ro
      - static_files:/app/staticfiles:ro
      - certbot_www:/var/www/certbot:ro
      - certbot_conf:/etc/letsencrypt:ro
      - nginx_logs:/var/log/nginx
    environment:
      NGINX_HOST: ${NGINX_HOST}
      NGINX_HOST_WWW: ${NGINX_HOST_WWW:-www.${NGINX_HOST}}
    depends_on:
      - web
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "5"

  certbot:
    image: certbot/certbot:latest
    volumes:
      - certbot_www:/var/www/certbot
      - certbot_conf:/etc/letsencrypt
    entrypoint: >
      sh -c "trap exit TERM; while :; do certbot renew --webroot -w /var/www/certbot --quiet; sleep 12h & wait $${!}; done"
    restart: always

  backup:
    build: .
    command: >
      bash -c "while :; do
        sleep 86400;
        /app/scripts/backup_db.sh;
      done"
    volumes:
      - .:/app
      - ./backups:/app/backups
    env_file:
      - .env.prod
    environment:
      DJANGO_ENV: prod
    depends_on:
      - db
    restart: always

volumes:
  postgres_data:
  static_files:
  certbot_www:
  certbot_conf:
  nginx_logs:
